# Состояние развёртывания

Проверено 25 сентября 2026 года по Москве (24 сентября, 21:xx UTC).

## Установка

- Адрес: https://paperclip.kartamyshev.dev
- Сервер: Ubuntu 24.04.5 LTS, x86_64, 3.8 GiB RAM; каталог `/opt/paperclip`.
- Docker Engine 29.8.1; Compose 5.5.1, Docker включён в автозапуск.
- Paperclip: 2026.916.1, commit `d554c4789ed3930f8a53ac9fdf6503b3187097da`.
- Режим: `authenticated/public`, PostgreSQL 17, Caddy, постоянные bind mounts в `data/`.
- Сертификат Let's Encrypt YE1: домен `paperclip.kartamyshev.dev`, действителен до 23 декабря 2026, 20:16:08 UTC. Продление выполняет Caddy.

Закреплённые образы:

| Сервис | Образ |
| --- | --- |
| Paperclip | `ghcr.io/paperclipai/paperclip@sha256:a02ac35ac41df911af477422ea0e781cf41d2b2c600c66f0a5ac9d8c63f52c2c` |
| PostgreSQL | `postgres@sha256:b0f9560a2de083e2cc7382e75f808c7381a32852a7ec49117deedb300e552b24` |
| Caddy | `caddy@sha256:6aeddd44c3078b0f9a35206472a11420648a79c184603ef95957d0a20044cb2b` |

## Выполненные проверки

- Google DNS, Cloudflare DNS и резолвер сервера возвращают адрес сервера.
- Конфигурация Compose валидна. Все три сервиса работают; Paperclip и PostgreSQL проходят healthcheck.
- `/api/health`: HTTP 200, `status=ok`, `authenticated/public`, нужный commit.
- HTTP возвращает 308 на HTTPS; TLS проверен без отключения валидации сертификата.
- `/auth`: HTTP 200 с HTML приложения; JavaScript и CSS из этой страницы также возвращают 200 с правильными Content-Type. Анонимный `/api/companies`: HTTP 403.
- Все контейнеры удалены и созданы заново без удаления данных; после этого health успешен, bootstrap-приглашение активно.
- Создана полная копия `20260924T211550Z`: контрольные суммы совпали, дамп успешно восстановлен в отдельную временную БД — 210 таблиц public и одно приглашение.
- Каноническое JSON-представление содержимого всех пользовательских таблиц восстановленной БД совпало с рабочей БД. Конфигурация из файлового архива совпала с постоянным файлом после перезапуска. Временная БД удалена.
- `paperclip-backup.service` завершился с `SUCCESS`; таймер включён, расписание 00:15 UTC плюс до 5 минут задержки, хранение 7 суток.
- Локальный и серверный `.env`: права `0600`; `.env`, `private/`, `backups/` исключены из Git.
- Начальное потребление памяти: Paperclip ~829 MiB, PostgreSQL ~103 MiB, Caddy ~12 MiB.

## Передача владельцу и ограничения

- Одноразовое приглашение сохранено только локально в `private/admin-invite.txt`, права `0600`; истекает 27 сентября 2026 в 21:15:03 UTC (28 сентября, 00:15 МСК).
- Учётная запись владельца ещё не создана; регистрация и принятие приглашения остаются пользователю. Вход с реальной учётной записью не проверялся.
- Встроенный браузер на Mac вернул `ERR_NAME_NOT_RESOLVED`. Поэтому визуальная проверка интерфейса не выполнена; HTTP/TLS проверены с сервера и с Mac через явное сопоставление домена с IP без отключения TLS-проверки. Настройки DNS Mac не менялись.
- AI-агенты и ключи провайдеров не подключены.
- Резервные копии находятся на том же сервере. Внешнее хранение и уведомления об ошибках копирования не настроены; статус доступен через systemd/journalctl.
- Копирование кратковременно останавливает приложение. Штатное встроенное копирование БД отключено, используется единый systemd-процесс для согласованной копии БД и файлов.

Инструкция эксплуатации и восстановления: `README.md`.
