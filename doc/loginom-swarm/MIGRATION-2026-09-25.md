# Миграция в LAN — выполнение 2026-09-25

Согласованный план: [план миграции](../plans/2026-09-25-lan-migration.md).
Ветка `swarm-lan-migration` от `ca32e9d2c615f13d2e27d72bb2b2455a589bca15`.

## Текущая контрольная точка

Источник `82.22.23.10` заморожен: worker, memory, Caddy и Paperclip остановлены,
их автозапуск и таймер backup отключены. PostgreSQL также остановлен после снимка.
Активных heartbeat runs — 0. Переключение пока не выполнено.
Снимок `/opt/paperclip/migration-bundles/20260925-lan` завершён успешно.
Архив 11 401 001 398 байт; manifest 310 975 964 байт. Source fingerprint
и полный повторный inventory совпали. Начата потоковая передача на назначение
в `/var/lib/loginom-migration/incoming/20260925-lan`; source остаётся замороженным.
Sampling не запускался. Остаётся запрет на повтор CLI CSV и cleanup до reconciliation.

## Выполнено на назначении

- SSH и sudo проверены: `10.200.4.106`, Ubuntu 26.04, 16 CPU, 31063 MiB RAM.
- Удалена прежняя согласованная ненужная установка Paperclip с её Docker volumes.
- Раздел ext4 расширен до доступного диска 200 GiB; свободно около 173 GiB.
- Имя машины `loginom-swarm`; корпоративный DNS возвращает адрес
  `10.200.4.106` для `loginom-swarm.bg.local`. Будущий интерфейс — HTTP, порт 80.
- Созданы отдельные системные учётные записи с UID/GID 21001–21004.
- Установлены системные зависимости исполнителей; подготовлена сеть `swarm-lan`
  с интерфейсом `br-swarm`, адресом `172.28.0.1/24`.
- Загружены три образа с исходными закреплёнными digest. Данные ещё не восстановлены.
- Установлен sing-box 1.12.25, архив проверен по официальной контрольной сумме.
  Используется новый профиль Trojan WebSocket TLS из предоставленной подписки;
  секреты находятся отдельно в защищённых файлах.
- Выходной IP `185.21.15.251` подтверждён двумя сервисами с хоста и из контейнера
  Paperclip. Настроены постоянные DNS, firewall и systemd зависимости.
- Проверка остановленного VPN: HTTPS и прямой публичный DNS блокируются.
  Первичный захват включал входящий multicast других LAN-машин; его результаты
  не являются доказательством утечки. Проверка только исходящего трафика подтвердила отсутствие утечки.

## Следующие этапы

Проверки отказа/перезагрузки VPN и пробный restore БД выполнены. Источник
заморожен, полный холодный снимок создан и передан. Завершить проверку и
восстановление файлов в staging, продвинуть их при закрытом допуске сервисов,
затем применить новый CPU перезагрузкой. Далее восстановить БД, настроить HTTP
и сервисы, проверить изоляцию, память, сохранность настроек и backup.
Источник сохранить замороженным для отката минимум семь дней после переключения.

## Ограничения

CLI 0.1.16 остаётся заблокированным после неподтверждённой мутации; пользователь
обновит CLI отдельно. Миграция не завершает реализацию полного координатора узла
и не даёт разрешение запускать Sampling. OAuth-профили включены в переданный холодный снимок; их работа на назначении
ещё не проверена.
Секреты не коммитить; файлы `private/` и `.env` исключены из Git.
Субагенты работают с локальными компонентами, серверы меняет основной агент.

## Дополнительные подтверждённые проверки

Перезагрузка назначения: SSH, Docker, VPN и firewall восстановились; exit IP PASS.
Остановка VPN: HTTPS и DNS blocked, исходящих публичных пакетов 0.
Пробный restore базы из backup 20260925T071935Z: 211 таблиц; полный row fingerprint совпал.
Изолированный контейнер и его volume после проверки удалены.

Проверка аварийного завершения sing-box: автоматический перезапуск и VPN exit PASS.
Локальные проверки: 64 Python tests, 16 VPN tests, upstream registry PASS.
Полный typecheck/build форка на этом этапе не выполнялись; рабочий образ при переносе не меняется.

DNS/VPN на Mac пользователь настраивает самостоятельно (подтверждено 25.09).
Корпоративный DNS 10.150.0.1 возвращает правильную A-запись; системный resolver Mac
пока не возвращает её. Настройки Mac не изменять. Для проверок использовать
прямой адрес назначения с Host loginom-swarm.bg.local.

## Уточнение сетевой защиты

Исправлена обработка UDP/ICMP, отмеченных sing-box: окончательная маршрутизация
происходит после OUTPUT. Разрешение узкой метки дополнено POSTROUTING с запретом
прямого публичного выхода после маршрутизации и SNAT. Проверены host/container
exit IP, NTP через VPN и отказ специально отмеченному UDP без VPN-маршрута.
При остановленном VPN исходящих публичных пакетов 0. Штатный chrony
выбрал Ubuntu NTS источник; NTPSynchronized=yes. Дополнительные тестовые
NTP источники удалены, изменения chrony.conf не требуются.

Реальный Linux smoke новой backup manifest: GNU tar + ACL + xattr roundtrip PASS.
/tmp на назначении — tmpfs 16 GiB; полная проверка backup использует временный
каталог на диске внутри /opt/paperclip/backups.

Подготовительные изменения опубликованы: draft PR #2, commit e6785fd32.
CI contracts PASS; полные application/tests/image проверки ещё выполняются.
Повторный reboot после исправления UDP: VPN/SSH/NTS PASS. LAN HTTP80 проверен
через одноразовый контейнер с Mac; контейнер удалён после успешной проверки.

Снимок содержит 803 875 записей. Передача выполняется private/stream_migration.py;
большой архив на Mac не сохраняется. На назначении запущена
loginom-migration-verify.service: ждёт атомарного появления полного архива, затем
проверяет SHA256 и каждую запись, извлекает только в новый staging
/var/lib/loginom-migration/restored и меняет служебные UID/GID.
Это не запускает приложение. Лог проверки: /var/lib/loginom-migration/verify.log (private).
После этого: продвижение проверенного staging, восстановление БД, настройка LAN
и живые проверки. Сервисы источника не включать параллельно назначению.

## Завершение передачи и предстоящая перезагрузка

Все пять файлов снимка переданы; filesystem.tar.gz — 11 401 001 398 байт.
Передающий процесс завершился с кодом 0. Проверка содержимого и восстановление
в staging продолжаются; пока перезагружать назначение нельзя. Пользователь
уже изменил профиль CPU виртуальной машины; применение требует перезагрузки.
После проверки и безопасной остановки операций сообщить пользователю. Затем
проверить AVX/AVX2, Bun revision/version и CLI --version без запуска Loginom задач.

CI для e6785fd32: application, image, contracts и пять групп тестов PASS;
general-server shard 0 FAIL. Причина исследуется; общий CI не считается успешным.

Перед продвижением staging добавлены проверки всех remapped владельцев,
отсутствия любых контейнеров проекта (включая stopped), сохранение метаданных
системных каталогов и постоянные migration gates для worker/memory/backup.
Gate открывать только после DB restore и подготовки к проверкам; backup timer
включать отдельно после успешной проверки новой резервной копии.

CI: два отказа в неизменённом upstream chat-channels.integration.test.ts
(Slack callbacks и GitHub late edit); связь с миграцией не подтверждена.
Запущен один повтор упавшей группы run 36132477971.
