# Безопасная остановка Loginom Swarm — 25.09.2026

Остановлено по прямому запросу пользователя. Проверка состояния: 2026-09-25T06:20:25+00:00.
**Работу продолжать только после нового запроса пользователя. Sampling не запускать.**

## Состояние при остановке

- VPS 82.22.23.10: worker и шлюз памяти active, тяжёлая блокировка свободна,
  активных execution jobs и обнаруженных процессов проверки Codex нет.
- Sampling case `038456b1-e389-4724-a212-436bb4dbd197`: `setup`, `manualStart=false`.
  Никакая обработка узла не запускалась. Роли остаются подготовительными.
- Production Paperclip остаётся на прежнем официальном digest; новый образ форка
  не опубликован и не установлен. Данные и существующие идентификаторы сохранены.
- Рабочий репозиторий `/Users/kartamyshev/Git/loginom-swarm`, ветка `swarm-bootstrap`.
  Последний ранее опубликованный commit `18488e95507bdda3d867d5da40bd758cdfe3c79f`.
  Текущие изменения **не закоммичены и не отправлены**. Не терять untracked файлы.
- Draft PR: https://github.com/kartamyshev-dev/loginom-swarm/pull/1 .
- Реквизиты находятся в корневом `.env`; не печатать и не переносить в память.
  Старый `/Users/kartamyshev/Git/paperclip` сохранён.

## Выполнено после аудита

### Исполнение и штатные cases

Добавлены `runtime/jobs.py`, `protocol.py`, `coordinator.py`, расширены worker/RPC.
Долговечные fsync receipts вне model mounts, requestKey привязан к payload,
повтор не создаёт второй процесс, одна тяжёлая операция, отмена с проверкой cleanup.
После прерывания worker неизвестный cleanup блокирует продолжение без возрастного
снятия locks. Координатор пока выполняет **только инфраструктурные probes**:
это не законченный координатор разработки узла.

Live native infrastructure case `bf86ca83-7f9e-4160-9a80-bdb47c0f3f5f` прошёл
`developer → reviewer → acceptance → done`. Повтор и повтор после рестарта не
запустили новые операции. Отмена fixed cancellation-probe дала cancelled и
cleanupConfirmed=true. Локальный receipt: `private/coordinator-qualification.json`.

Sampling pipeline `c92742f4-f55a-42e3-9c3b-93b58da4f2a6` получил 32 явных ребра
переходов через API. Рабочие этапы отключены; setup/blocked/cancelled остаются
доступными. Reviewer задан явным approver. Case не переходил из setup.

Корневой runtime `/opt/loginom-swarm/runtime`, обновление с rollback
`/opt/loginom-swarm/rollbacks/20260925T055056Z`. Затем адресно установлены memory
observer и sandbox. Последние локальные мелкие исправления jobs/coordinator и
bootstrap installer ещё не синхронизированы целиком с сервером.

### Общая память

Развёрнут HTTP gateway `127.0.0.1:8766`, systemd `loginom-swarm-memory`, отдельный
пользователь loginom-memory. Master credential находится только в
`/etc/loginom-swarm/memory-gateway.json` (root:loginom-memory,0640), worker его
не читает. Строгая политика разрешает только канонический Peer и собственную
сессию. Неопределённый ответ на запись блокирует новые мутации до разбора.
Gateway state: `/var/lib/loginom-swarm-memory`.

Новая серверная генерация `20260925.1` в `/opt/loginom-swarm/memory`, исходник
`swarm/memory`. Локальная `20260924.1` основного проекта не изменялась.
12 vendored модулей официального OpenViking plugin0.8.1, Apache LICENSE,
исходные/развёрнутые hashes в provenance.json. Патчи запрещают захват aged lock,
сброс corrupt/unreadable state и движение cursor назад при shrink transcript.
Не все крайние случаи этих патчей ещё покрыты проверками.

Пять штатных hooks доверены через Codex hooks/list + config/batchWrite с actual
hashes. **Никакого bypass hook trust**. Настройки и hooks после enrollment
скопированы в root-owned `memory-sealed/<role>` и монтируются read-only.
SessionEnd ограничен самим Codex 3 секундами: capture и commit выполняются в Stop
и PreCompact; SessionEnd записывает только факт закрытия. Полноценная сверка
последнего cursor при SessionEnd пока не реализована, не считать её проверенной.

Реальные подготовительные сессии Astra medium:

| Роль | Codex thread ID | Проверенный memory turn |
|---|---|---|
| developer | `01a0d730-48e3-7563-a21e-00cd09b23daa` | `01a0d733-d25d-7592-a8f2-78989c93c47e` |
| reviewer | `01a0d731-87cd-7b31-b75d-527b60aec7c4` | `01a0d734-e8d9-7461-ae52-794257ac170e` |

Подтверждены настоящие SessionStart, trusted hooks, завершённый подготовительный
ход, последующее resume тех же сессий, recall/capture/commit обеих ролей.
Серверные receipts: `/opt/loginom-worker/state/memory-bootstrap-<role>.json` и
`memory-qualification-<role>.json`. Они вне model mounts.
Регистрации `/etc/loginom-swarm/memory-roles/<role>.json`: active; это не означает
готовность всей кампании. Секреты профилей `/opt/loginom-worker/profiles/sampling`.

Через зарегистрированные OpenViking tools текущей задачи на Mac выполнены find и
read точной извлечённой серверной записи:
`viking://user/kartamyshev/peers/-Users-kartamyshev-Git-loginom-ai-agent/memories/events/2026/09/25/swarm_memory_acknowledge.md`.
Там присутствует `SWARM_MEMORY_20260925` и фактическое устройство исполнителей.
Это подтверждает extraction и чтение из общего Peer. Отдельная проверка именно
из уже существующей основной задачи loginom-ai-agent ещё не выполнялась.

**Непосредственная точка остановки:** только что созданы локальные
`swarm/memory/mcp.py` и `swarm/scripts/configure-memory-tools.py`.
Это stdio инструменты find/read через gateway; они **ещё не проверены, не
установлены на VPS и не подключены к Codex**. Сейчас recall идёт через hook,
явных MCP инструментов у серверных ролей ещё нет.

## Проверки и незавершённые действия

- 30 Python tests PASS после первого развёртывания gateway/enrollment. После
  этого добавлены ещё 3 проверки session GET/vendor integrity/lock+corrupt state
  и небольшие исправления; полный набор повторно **не запускался**.
- Новый MCP facade не тестировался. Нужны протокольные тесты и реальная проверка
  tools/list/call через pinned Codex, отрицательные чужой Peer/acceptance проверки.
- Последний проверенный GitHub CI для 18488e955: contracts и image success,
  application cancelled. Полного зелёного CI нет; PR не готов к завершению.
- `install-host-worker.sh` локально исправлен для установки новых runtime modules.
  `install-memory-gateway.sh` пока устанавливает только Python transport, нужно
  дополнить установкой hook/vendor/MCP и воспроизводимым enrollment runbook.
- Backup пока **не включает новый gateway state, memory code и memory unit**.
  `/etc/loginom-swarm` уже входит. До следующей готовности расширить backup,
  согласованно останавливая gateway после получения heavy lock, затем проверить
  restore конфигурации/receipts/sealed files. Последний прежний backup
  `20260925T011019Z`; предыдущие proofs в VALIDATION.md.
- Перед дальнейшими тестами уточнить ограничения capture: empty/shrunk rollout,
  crash после commit до cursor/receipt, незавершённый Stop, fail-closed SessionEnd.
  Никогда не фабриковать успешный capture receipt и не удалять locks по возрасту.
- Доработать docs/manifest и реестр собственных компонентов, сохранить изменения
  в commit и обновить draft PR после соответствующих проверок. Upstream core
  текущим increment не менялся. Не делать merge/release/deploy автоматически.

## Порядок продолжения

1. Прочитать этот файл, CHECKPOINT.md, AGENTS.md; сверить git status и live state.
2. Закончить и проверить MCP find/read, установить только проверенный runtime,
   обновить sealed config операторским инструментом и проверить hooks trust.
3. Проверить продолжение тех же thread ID после рестарта, отрицательные сценарии
   и обратное чтение; расширить backup и проверить восстановление новых данных.
4. Проверить весь текущий собственный increment, обновить документацию и draft PR.
5. Затем реализовывать настоящий model stage runner/coordinator, полную сборку
   кандидата из итогового commit, независимую Loginom приёмку save/reopen,
   контролируемый personal publisher, лимиты/ошибки и квалификацию ресурсов.
6. Добиться зелёного CI, проверить upstream-update workflow, отдельно собрать и
   развернуть образ форка с backup/rollback. Только после всех gate подготовить
   ручной старт Sampling в UI. Сам узел запускает пользователь.

Личный GitHub OAuth уже работает под kartamyshev-dev: тестовые push/PR2 в
`gooddaytoday/loginom-ai-agent` подтверждены ранее, PR закрыт без merge.
Установка GitHub App владельцем больше не требуется. Автоматический publisher
ещё не подключён. Отдельные OAuth dev/reviewer/acceptance уже выполнены на VPS.
Node24.19.0/Bun1.3.14/CLI0.1.16 и полная инфраструктурная source build ранее
проверены; тяжёлая приёмка и её ресурсный пик ещё не проверены.
